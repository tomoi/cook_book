import UserDropdown from '../userDropdown/UserDropdown';

export default function Header() {
    return (
        <nav>
            <h2>Header Here</h2>
            <UserDropdown />
        </nav>
    );
}
